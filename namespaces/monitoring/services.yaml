---
apiVersion: v1
kind: Service
metadata:
  name: grafana-webui
  namespace: monitoring
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 30080
  selector:
    app: grafana
